To build and install these patched modules:

1. export KBUILD_MODPOST_WARN=1
2. make ccflags-y="-I\$(src)/linux -I\$(src)"
3. sudo make install
4. sudo modprobe -v vmmon
5. sudo modprobe -v vmnet
6. sudo systemctl restart vmware
